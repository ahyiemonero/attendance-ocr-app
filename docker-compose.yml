services:
  attendance-ocr:
    build: .
    container_name: attendance-ocr
    restart: unless-stopped
    ports:
      - "7869:7869"
    volumes:
      - attendance_uploads:/app/uploads
      - attendance_generated:/app/generated
      - attendance_data:/app/data

volumes:
  attendance_uploads:
  attendance_generated:
  attendance_data: